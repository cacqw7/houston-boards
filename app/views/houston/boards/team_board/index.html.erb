<h4><%= @board.name %> Board</h4>

<% if @board.lists.any? %>
  <% @board.lists.each do |list| %>
    <%= react_component('List', { name: list.name }, {prerender: true }) %>
  <% end %>
<% else %>
  <%= link_to "Add a Column!" %>
<% end %>
